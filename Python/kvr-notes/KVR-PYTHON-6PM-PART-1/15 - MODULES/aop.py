#Program for add and sub of two numbers
#aop.py-----File Name----Every File Name is internally Treated as Module
def  addop(a,b): # Function Def
	print("\tsum({},{})={}".format(a,b,a+b))

def  subop(a,b): # Function Def
	print("\tsub({},{})={}".format(a,b,a-b))


